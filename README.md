# Screen-Recoreder-Using-OpenCV-Python

## Steps to use
1) Download code
2) Make sure you have the following libraries installed :
      ``pip install numpy``
      ``pip install opencv-python``
      ``pip install PyAutoGUI``
